import { Trans } from '@lingui/macro';
import * as React from 'react';

import { TopInfoPanel } from '../../../components/TopInfoPanel/TopInfoPanel';

export const TGETopPanel = () => {
  return <TopInfoPanel pageTitle={<Trans>TGE</Trans>} />;
};
